<tr>
    @foreach ($keys as $key)
        <th class="bg-black border-r border-b border-gray-600 px-2 py-3 text-center text-sm font-semibold">
            {{ $key }}
        </th>
    @endforeach
</tr>
