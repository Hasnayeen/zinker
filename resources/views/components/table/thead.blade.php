<tr>
    @foreach ($keys as $key)
        <th class="bg-slate-800 border-r border-y border-gray-600 px-2 py-3 text-center text-sm font-semibold">
            {{ $key }}
        </th>
    @endforeach
</tr>
